---
title: Releases
description: Track this port’s versions separately from Base UI React.
---

Publish and changelog details for **base-ui-svelte** live in this repository (npm package `base-ui-svelte` and optional `@base-ui-svelte/styles`).

Base UI React release notes do **not** automatically apply here. When APIs diverge, we document them under [Differences](/handbook/differences).

Upstream React history: [Base UI Releases](https://base-ui.com/react/overview/releases).
