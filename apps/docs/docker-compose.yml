# Docs static site — use with Dokploy Compose / Docker
services:
  docs:
    build:
      context: ../..
      dockerfile: apps/docs/Dockerfile
      args:
        PUBLIC_SITE_ORIGIN: ${PUBLIC_SITE_ORIGIN:-https://docs.example.com}
    ports:
      - '3000:80'
    restart: unless-stopped
