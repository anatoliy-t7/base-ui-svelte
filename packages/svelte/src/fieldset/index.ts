import Root from './fieldset-root.svelte';
import Legend from './fieldset-legend.svelte';

export const Fieldset = {
	Root,
	Legend,
};

export type { FieldsetRootProps, FieldsetLegendProps, FieldsetContext } from './types.js';
