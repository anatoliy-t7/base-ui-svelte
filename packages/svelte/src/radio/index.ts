import Root from './radio-root.svelte';
import Indicator from './radio-indicator.svelte';

export const Radio = {
	Root,
	Indicator
};

export type { RadioRootProps, RadioIndicatorProps, RadioContext } from './types.js';
